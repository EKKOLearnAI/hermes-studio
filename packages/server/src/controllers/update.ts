// Compatibility facade. The Studio-owned update feature is composed in bootstrap.
export {
  handleUpdate,
  installPreview,
  preparePreview,
  previewStatus,
  previewTags,
  startPreview,
  stopPreview,
} from '../bootstrap/update'
