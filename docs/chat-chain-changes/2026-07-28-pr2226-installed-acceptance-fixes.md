---
date: 2026-07-28
pr: 2226
feature: Mixed-runtime Group Chat installed-acceptance fixes
impact: Claude Code Group Chat turns now use exactly one system-prompt mechanism while ordinary scoped Claude Code sessions retain their prompt-file launch path; runtime reuse additionally requires an exact ordinary-versus-Group-Chat context match, so a shared Session ID cannot retain unsafe ordinary launch flags, suppress the trusted Room prompt, or reuse either the context-bound agent Session identity or native conversation identity. An incompatible live Runtime must complete bounded synchronized shutdown before an ordinary/Group Chat replacement may launch; unconfirmed shutdown fails closed. Fixed-order successors receive the same-Room durable chain-root user request separately from predecessor output across Hermes, Codex, and Claude Code runtimes; active fixed chains also pin that provenance-validated depth-0 user request during Room retention, so a pending successor cannot lose its original task to pruning. Native Hermes receives a typed single-line envelope plus trusted sink instructions so predecessor text remains untrusted data. Every leased publication must preserve the durable source job's chain ID and exact successor depth, and any successor plan must retain that same provenance, preventing same-Room task substitution before terminalization. Room deletion, participant deletion, and context clearing acquire a SQLite-visible, token-owned runtime-mutation fence before stopping runtimes. Active mutations renew the exact `token + roomId + actorId` lease throughout asynchronous runtime shutdown, and both renewal and release also validate the derived Room-versus-participant fence kind, keeping admission, claim, prelaunch, and publication blocked past the original TTL; renewal loss fails closed, heartbeat timers are stopped and token-owned fences are released on every exit even if a post-commit runtime finalizer throws. Room/context cleanup uses nested finalizers so a throwing local pause release cannot skip the participant-session fence release, while bounded expiry still permits recovery after a crashed owner. Participant deletion additionally holds a reference-counted local Room admission pause from fence acquisition through its guarded deletion transaction and retained runtime cleanup, while participant removal drops its in-memory client and queued mentions before the pause is released, so overlapping runtime mutations cannot re-enable handoffs or replay stale work under a reused stable agent ID. Handoff enqueue, claim, lease-current/prelaunch, and late publication checks fail closed while the relevant Room or actor fence is active; each Room serializes destructive runtime mutations, and bounded fence leases with route-owned renewal allow crash recovery without silently admitting work during a live or long-running mutation; a lost/expired fence makes the operation return conflict before persistence changes. These operations cancel existing handoffs, synchronize every exact affected target runtime—including persisted participants with no in-memory client—and require an explicit synchronized Bridge result (an unknown/missing persisted Bridge Session is not treated as proof of stop) before mutation, retain the per-Room execution pause until the guarded SQLite mutation finalizes, reject any complete participant-binding tuple or fence-token drift observed between stop and commit, and release fences on every conflict or exception path. Only after the guarded transaction commits may runtime state disconnect/reset and success events emit. The same transaction atomically removes only ownership-matching source=group_chat Coding Agent Studio sessions, messages, compression snapshots, and workspace-change evidence while retaining usage audit rows and unrelated single-chat sessions. Context clearing performs this cleanup against the old persisted participant tuple in the same transaction before rotating each Session generation, so stopped runtimes cannot leave hidden orphan Sessions behind. If another participant in the same or another Room still references the same Session ID, both runtime interruption and destructive Session cleanup fail closed so the surviving participant and its evidence remain intact. Participant deletion fences jobs where that actor is initiator, source, or target without revoking participant capabilities before runtime synchronization; only a successful exact-binding CAS revokes authority and deletes identity in the same SQLite transaction. Room deletion similarly captures the Room revision and complete participant runtime-binding set, holds the Session fence through stop-and-wait and the durable CAS delete, and disconnects in-memory Room state only after that delete commits. Context clearing uses the same guard and two-phase finalizer: old Session cleanup and generation rotation commit under the exact stopped binding set before in-memory context reset and clear events are emitted. Either path returns conflict with participant/Room identity and Session evidence preserved when synchronization or the CAS cannot be proven. Legacy persisted Codex and Claude Code sessions are ownership-reclassified during schema initialization only when a complete live participant tuple proves the same Session ID, profile, coding_agent runtime, and explicit codex↔codex or claude-code↔claude mapping; ledger-only or mismatched historical rows remain ordinary Coding Agent sessions.
---
